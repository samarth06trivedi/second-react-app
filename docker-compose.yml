version: '3.8'

services:
  vite-app:
    build: .
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: ['npm', 'run', 'preview'] # Ensure this matches your Dockerfile CMD
